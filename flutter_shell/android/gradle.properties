org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Pub cache (C:) and project (D:) live on different drives; the Kotlin
# incremental compiler cannot compute relative paths across drives and falls
# back to full recompiles with a flood of errors. Disable incremental to
# compile cleanly. Safe to remove once the cache and project share a drive.
kotlin.incremental=false
