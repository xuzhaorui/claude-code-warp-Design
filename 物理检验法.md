---
title: 物理检验法
date: 2026-06-26
tags:
  - AI开发
  - 方法论
  - 远程工作冲刺
  - 工程效率
aliases:
  - 物理检验法
  - 去文档税开发法
---

# 物理检验法

## 先质疑原问题

原问题不是“如何让顶级模型写更细的开发文档，再交给便宜模型执行”。

这个问题的错误前提是：**细文档能降低成本**。

实际相反。顶级模型最贵、最慢的部分就是长文本输出；小模型再读取这些长文档，又会产生新的输入成本和理解偏差。结果不是降本，而是多交一层“中间文档税”。

真正的问题是：

> 如何把人类的模糊意图，压缩成机器可执行、可校验、可反复迭代的物理契约？

## 核心定义

**物理检验法**：用确定性的测试、编译器、Linter、设计令牌和运行环境，取代模糊自然语言文档与 AI 主观评估。

它追求的不是“让 AI 更懂我”，而是：

> 把系统尽可能拆成红灯 / 绿灯问题。

## 三层角色

### 1. 人类：提供第一因

人类不负责写细文档，而负责给出不可自动化的意志：

- 这个产品为什么存在
- 哪个业务约束绝不能错
- 审美方向是什么
- 哪些体验必须保留
- 哪些复杂度应该被删除

例子：

- “并发扣减同一个 SKU 时，库存绝不允许超卖。”
- “界面极简、暗色、高对比度，不要圆角和装饰。”

### 2. 顶级模型：把意志翻译成契约

顶级模型不写长篇开发文档，只输出机器可以校验的边界：

- 单元测试
- 接口定义
- 设计令牌
- Lint 规则
- 验收脚本
- 文件边界

它的价值不是执行快，而是把模糊意图降维成确定性规则。

### 3. 小模型 / Agent：在契约内高频试错

小模型不负责理解宏大系统，只负责让红灯变绿：

1. 写代码
2. 跑测试 / 编译 / Lint
3. 失败就读取报错
4. 修改
5. 重试

连续失败超过阈值，例如 3 次，再把错误日志、当前代码和契约打包给顶级模型纠偏。

## 最小闭环

```text
人类意图
  -> 顶级模型生成契约
  -> 小模型实现
  -> 物理环境校验
  -> 失败则带日志重试
  -> 多次失败才升级给顶级模型
```

关键是：**评估者不是另一个 AI，而是真实环境。**

## 什么应该被 Delete

- 几万字的开发细节文档
- “把 UI 做高级一点”这类模糊指令
- AI 评价 AI 是否完成
- 无法被测试或 Lint 消化的长篇解释
- 为了流程完整而存在的中间产物

不要优化一个本不该存在的环节。

## 后端怎么落地

后端的契约优先是测试。

例如“扫码出库”不要反复测试物理扫码枪，而是拆成两层：

- 核心逻辑：输入 `"SKU-123"`，库存扣减，事务正确，并发不超卖
- 外壳交互：摄像头 / 扫码枪是否能把条码输入系统

AI 高频闭环只负责核心逻辑。物理外壳由人类在集成阶段少量验证。

## UI 怎么落地

UI 的主观审美不能被 AI 物理校验，但可以被拆成客观参数。

不要说：

> 做得更高级、更克制、更有设计感。

要沉淀成：

```yaml
colors:
  surface: "#0A0A0A"
  textPrimary: "#E0E0E0"
spacing:
  base: "8px"
radii:
  button: "0px"
components:
  primaryButton:
    background: "{colors.surface}"
    color: "{colors.textPrimary}"
    radius: "{radii.button}"
```

然后用 Linter / AST 检查约束：

- 禁止硬编码颜色
- 禁止未定义 spacing
- 禁止组件绕过 token
- 检查对比度
- 检查未使用 token

UI 的方法不是让 AI 主观审美，而是让 AI 严格服从一套设计物理常量。

## DESIGN.md 的启发

`DESIGN.md` 的本质不是传统文档，而是“双层协议”：

- YAML：给机器执行的设计令牌，负责颜色、字体、间距、圆角、组件映射
- Markdown：给 AI 理解的上下文，负责解释这些令牌的使用边界

它的关键价值：

> 文档即代码，设计即契约。

只要这份文档能被 lint、export、引用，它就不是文档税，而是生产系统的一部分。

## Naval 连接

Naval 说聪明人的特点是：迭代、坚持、洞察全局。

物理检验法对应如下：

| Naval 的定义 | 工程对应物      | 本质          |
| --------- | ---------- | ----------- |
| 迭代        | 小模型 + 编译器  | 低成本高频试错     |
| 坚持        | Agent 自动重试 | 把报错当作下一轮输入  |
| 洞察全局      | 顶级模型       | 只在边界和死胡同处介入 |

聪明不是一开始写出完美方案，而是建立一个能快速逼近正确答案的系统。

## 一句话方法论

> 人类给方向，顶级模型写契约，小模型写实现，真实环境裁判。

## 使用模板

每个任务只需要回答 5 个问题：

1. 人类意图是什么？
2. 哪部分能变成测试？
3. 哪部分能变成配置 / token / lint 规则？
4. 哪部分必须由人类最终判断？
5. 小模型失败几次后升级给顶级模型？

如果一个环节既不能产生代码，也不能产生测试，也不能产生规则，它大概率就是文档税。
